// Dependencies
import React from 'react';

// Screens
import LoginScreen from '../screens/LoginScreen';

const routes = [
  {
    path: '/',
    element: (<LoginScreen />)
  }
];

export default routes;
